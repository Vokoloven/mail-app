import { Form } from 'components/form';
import { AdaptiveContainer } from 'components/container';

const Home = () => {
    return (
        <AdaptiveContainer>
            <Form />
        </AdaptiveContainer>
    );
};

export default Home;
